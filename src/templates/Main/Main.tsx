import { Container, Header } from 'components';

import * as S from './Main.styles';

export const Main = () => {
  return (
    <Container>
      <S.Wrapper>
        <Header items={[]} />
      </S.Wrapper>
    </Container>
  );
};
